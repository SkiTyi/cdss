fastapi>=0.115.0
uvicorn[standard]>=0.32.0
sqlalchemy>=2.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
httpx>=0.27.0
python-multipart>=0.0.12
openpyxl>=3.1.0
pandas>=2.0.0
python-dotenv>=1.0.0

# ── Model training dependencies ──────────────────────────────────────────────
# Install these if using local GPU training:
#   pip install torch --index-url https://download.pytorch.org/whl/cu124
#   pip install transformers peft trl accelerate datasets bitsandbytes
transformers>=4.45.0
peft>=0.13.0
trl>=0.12.0
accelerate>=1.0.0
datasets>=3.0.0
# bitsandbytes>=0.44.0   # uncomment for 4-bit quantization (Linux + CUDA only)
